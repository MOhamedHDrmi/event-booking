server:
  port: 8080

spring:
  application:
    name: event-booking
  datasource:
    url: jdbc:h2:mem:./event-booking
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
